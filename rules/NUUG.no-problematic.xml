<!--
	For rules that are on by default, see NUUG.no.xml.

-->
<ruleset name="NUUG.no (problematic)" default_off="mismatched" platform="cacert">

	<target host="planet.nuug.no" />
	<target host="wiki.nuug.no" />


	<rule from="^http://(planet|wiki)\.nuug\.no/"
		to="https://$1.nuug.no/" />

</ruleset>
