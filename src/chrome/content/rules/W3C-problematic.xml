<!--
Disabled by https-everywhere-checker because:
Fetch error: http://test.w3.org/ => https://test.w3.org/: (6, 'Could not resolve host: test.w3.org')

	W3C

	Nonfunctional subdomains:

		- flanders (mismatch)
		- herman (timeout)
		- irc (401)
		- services (mixed content)
-->
<ruleset name="W3.org (problematic)" default_off='see comments'>

 <!-- Serves different content depending on whether accessed via HTTP or HTTPS -->
	<target host="people.w3.org" />

 <!-- Redirects to https://github.com/w3c/validator-suite -->
	<target host="validator-suite.w3.org" />

	<rule from="^http:"
		to="https:" />

</ruleset>
