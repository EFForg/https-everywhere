<!--
Disabled by https-everywhere-checker because:
Fetch error: http://offlinemode.org/ => https://offlinemode.org/: (60, 'SSL certificate problem: self signed certificate in certificate chain')
-->
<ruleset name="offlinemode.org" default_off='failed ruleset test'>

	<target host="offlinemode.org" />
	<target host="*.offlinemode.org" />


	<!--	Cert doesn't match www.	-->
	<rule from="^http://(?:(mail\.)|www\.)?offlinemode\.org/"
		to="https://$1offlinemode.org/" />

</ruleset>
