<!--
Disabled by https-everywhere-checker because:
Fetch error: http://epochstats.com/ => https://epochstats.com/: (35, 'error:14077410:SSL routines:SSL23_GET_SERVER_HELLO:sslv3 alert handshake failure')
	For other Epoch coverage, see Epoch.com.xml.


	(www.): configured for rc4 only

-->
<ruleset name="EpochStats.com" default_off='failed ruleset test'>

	<target host="epochstats.com" />
	<target host="*.epochstats.com" />


	<rule from="^http://((?:hits|sales|www)\.)?epochstats\.com/"
		to="https://$1epochstats.com/" />

</ruleset>
