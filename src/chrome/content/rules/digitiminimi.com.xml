<!--

	Nonfunctional subdomains:

		- ^ 	(refused)
		- doyogi	(refused)
		- spanner1	(refused)
		- tampen	(refused)
		- tfm	(refused)
		- todo	(refused)
		- yoyogi	(refused)

	Problematic subdomains:

		- former	(invalid cert)
		- yoyogiblog	(invalid cert)

-->
<ruleset name="digitiminimi.com">

	<target host="jsoon.digitiminimi.com" />

	<securecookie host="^\w" name=".+" />

	<rule from="^http:"
		to="https:" />

</ruleset>
