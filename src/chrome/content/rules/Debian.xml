<!--
	Debian domain names with https services that have a cert
	signed by a CA recognized by default firefox.
-->

<ruleset name="Debian">
	<target host="*.debian.org"/>

	<rule from="^http://(wiki)\.debian\.org/"
		to="https://$1.debian.org/"/>
</ruleset>

