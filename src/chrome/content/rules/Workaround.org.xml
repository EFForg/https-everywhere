<ruleset name="workaround.org">

	<target host="workaround.org" />
	<target host="*.workaround.org" />


	<rule from="^http://(fry\.|www\.)?workaround\.org/"
		to="https://$1workaround.org/" />

</ruleset>
