<ruleset name="IPredator">

	<target host="ipredator.se"/>
	<target host="*.ipredator.se"/>

	<securecookie host="^(.*\.)?ipredator\.se$" name=".*"/>

	<rule from="^http://(blog\.|www\.)?ipredator\.se/"
		to="https://$1ipredator.se/"/>

</ruleset>
