<!--
	Fully covered subdomains:

		- (www.)
		- bugs
		- git
		- lists

-->
<ruleset name="pwmt.org (partial)" default_off="expired">

	<target host="pwmt.org"/>
	<target host="*.pwmt.org"/>

	<rule from="^http://((?:bugs|git|lists|www)\.)?pwmt\.org/"
		to="https://$1pwmt.org/"/>

</ruleset>
