<ruleset name="pwmt.org (partial)" default_off="expired">

	<target host="pwmt.org"/>
	<target host="www.pwmt.org"/>

	<rule from="^http://(www\.)?pwmt\.org/"
		to="https://$1pwmt.org/"/>

</ruleset>
