<!--
	For rules that are on by default, see GNOME.xml.

-->
<ruleset name="GNOME (expired)" default_off="expired">

	<target host="rt.gnome.org" />


	<!--	Cookies are handled in the main ruleset.	-->


	<rule from="^http://rt\.gnome\.org/"
		to="https://rt.gnome.org/" />

</ruleset>
