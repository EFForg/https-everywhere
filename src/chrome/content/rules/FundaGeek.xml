<ruleset name="FundaGeek (partial)" default_off="mismatch">

	<target host="*.fundageek.com"/>

	<securecookie host="^(?:mail|www)\.fundageek\.com$" name=".*"/>

	<rule from="^http://(mail\.|www\.)?fundageek\.com/"
		to="https://$1fundageek.com/"/>

</ruleset>
