
<!--
Disabled by https-everywhere-checker because:
Fetch error: http://directory.eoportal.org/ => https://directory.eoportal.org/: (60, 'SSL certificate problem: unable to get local issuer certificate')

	Nonfunctional subdomains:

		- www	(times out)


	^eoportal.org doesn't exist.

-->
<ruleset name="eoPortal (partial)" default_off='failed ruleset test'>

	<target host="directory.eoportal.org" />


	<securecookie host="^directory\.eoportal\.org$" name=".+" />


	<rule from="^http://directory\.eoportal\.org/"
		to="https://directory.eoportal.org/" />

</ruleset>