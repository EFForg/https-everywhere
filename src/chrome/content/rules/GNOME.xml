<!--
	Nonfunctional:

		- www-old		(cert: www.gnome.org, expired; 404)

-->
<ruleset name="GNOME (partial)">

	<target host="*.gnome.org" />

	<rule from="^http://(bugzilla|mail|live)\.gnome\.org/"
		to="https://$1.gnome.org/" />

</ruleset>
