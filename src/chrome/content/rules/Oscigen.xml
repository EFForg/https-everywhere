<!--
Disabled by https-everywhere-checker because:
Fetch error: http://oscigen.org/ => https://oscigen.org/: (6, 'Could not resolve host: oscigen.org')
Fetch error: http://www.oscigen.org/ => https://oscigen.org/: (6, 'Could not resolve host: www.oscigen.org')
-->
<ruleset name="Oscigen (CAcert)" platform="cacert" default_off='failed ruleset test'>

	<target host="oscigen.org"/>
	<target host="www.oscigen.org"/>

	<rule from="^http://(?:www\.)?oscigen\.org/"
		to="https://oscigen.org/"/>

	<securecookie host="^oscigen\.org$" name=".*"/>

</ruleset>
