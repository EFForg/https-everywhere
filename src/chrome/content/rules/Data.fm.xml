<ruleset name="data.fm">

	<target host="data.fm"/>
	<target host="*.data.fm"/>

	<securecookie host=".*\.data\.fm$" name=".*"/>

	<rule from="^http://data\.fm/"
		to="https://data.fm/"/>

	<rule from="^http://([\w\-_]+)\.data\.fm/"
		to="https://$1.data.fm/"/>

</ruleset>
