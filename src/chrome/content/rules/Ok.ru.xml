<!--
	(www.)?: redirects to http

	v: expired 2014

-->
<ruleset name="Ok.ru (partial)" default_off="expired, missing certificate chain, untrusted root">

	<!--	Direct rewrites:
				-->
	<target host="v.ok.ru" />

		<!--	Redirects to http:
						-->
		<!--exclusion pattern="^http://(www\.)?ok\.ru/($|dk\?st\.cmd=redirectAuthCode&amp;nc=\d+|favicon\.ico|favicon\.ico\?v2)" /-->


	<rule from="^http:"
		to="https:" />

</ruleset>
