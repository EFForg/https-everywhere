<!--
	Nonfunctional subdomains:

		- downloads	(refused)

-->
<ruleset name="Xiph.org">

	<target host="xiph.org" />
	<target host="*.xiph.org" />


	<securecookie host="^.*\.xiph\.org$" name=".*" />


	<rule from="^http://((?:git|svn|wiki|trac|www)\.)?xiph\.org/"
		to="https://$1xiph.org/" />
</ruleset>
