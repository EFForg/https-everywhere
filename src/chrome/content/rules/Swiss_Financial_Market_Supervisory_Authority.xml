<!--
Disabled by https-everywhere-checker because:
Fetch error: http://finma.ch/ => https://www.finma.ch/: (6, 'Could not resolve host: finma.ch')
-->
<ruleset name="Swiss Financial Market Supervisory Authority (FINMA)" default_off='failed ruleset test'>
    <target host="finma.ch" />
    <target host="*.finma.ch" />

    <rule from="^http://finma\.ch/"
        to="https://www.finma.ch/"/>
    <rule from="^http://([^/:@]+)?\.finma\.ch/"
        to="https://$1.finma.ch/" />
</ruleset>
