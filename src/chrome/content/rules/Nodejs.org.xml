
<!--
Disabled by https-everywhere-checker because:
Fetch error: http://packages.nodejs.org/ => https://packages.nodejs.org/: (51, "SSL: no alternative certificate subject name matches target host name 'packages.nodejs.org'")

	For other Joyent coverage, see Joyent.xml.


	Nonfunctional subdomains:

		- blog ¹
		- build *
		- logs *

	¹ Shows ^nodejs.org
	* Refused


	Problematic subdomains:

		- jobs ¹
		- www ²

	¹ Mismatched, CN: *.simply-partner.com
	² Cert only matches ^nodejs.org


	Mixed content:

		- css on jobs from fonts.googleapis.com *

	* Secured by us

-->
<ruleset name="nodejs.org (partial)" default_off='failed ruleset test'>

	<!--	Direct rewrites:
				-->
	<target host="nodejs.org" />
	<target host="packages.nodejs.org" />

	<!--	Complications:
				-->
	<target host="www.nodejs.org" />


	<rule from="^http://www\.nodejs\.org/"
		to="https://nodejs.org/" />

	<rule from="^http:"
		to="https:" />

</ruleset>
